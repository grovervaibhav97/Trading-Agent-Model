# Trading Agent Model

This project simulates a simple trading agent that buys and sells stocks based on price data.

## Project Structure

data.csv → stock price data  
environment.py → trading environment  
agent.py → trading decision logic  
run.py → run the simulation  

## How to Run

1. Install dependencies:
   pip install -r requirements.txt

2. Run the project:
   python run.py

